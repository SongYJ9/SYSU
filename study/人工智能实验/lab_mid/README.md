## 期中实验：卷积/循环神经网络

可以使用框架写网络啦（不用框架正常人根本做不出来）

入门的话这边还是建议使用 pytorch（tensorflow 巨难）

CNN 主要用于二维数据（如图像），即四个方向相邻几个数据关系很大的那种类型的数据

RNN 主要用于序列数据（如文本），即前后方向相邻几个数据关系很大的那种类型的数据

当然以后会有那种三维数据（如视频），可以考虑 CNN+RNN 一起玩耍~